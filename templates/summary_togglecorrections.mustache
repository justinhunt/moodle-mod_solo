
    <div class="mod_solo_toggle_differences" id="{{uniqid}}_differencetoggle">
        <a href="#" class="mstfd_showall" id="{{uniqid}}_mstfd_showall" style="display: none">{{#str}}showcorrections, mod_solo{{/str}}</a>
        <a href="#" class="mstfd_hidecrossouts" id="{{uniqid}}_mstfd_hidecrossouts">{{#str}}hidecorrections, mod_solo{{/str}}</a>
    </div>

{{#js}}
        //<script>
            require(['jquery','core/log'], function($, log) {
                  $("#{{uniqid}}_differencetoggle").on('click',function(e){
                      log.debug('clicked');
                      e.preventDefault();
                      $("#{{uniqid}}_mstfd_showall").toggle();
                      $("#{{uniqid}}_mstfd_hidecrossouts").toggle();
                      var crossoutshidden = $("#{{uniqid}}_mstfd_hidecrossouts").css('display') === 'none';

                      if (crossoutshidden) {
                          // Hide text within <del> tags , highlight inserted text
                          $('.mod_solo_corrections_cont .mod_solo_grading_original_preword').hide();
                          $('.mod_solo_corrections_cont .mod_solo_grading_original_postword').hide();
                      } else {
                          $('.mod_solo_corrections_cont .mod_solo_grading_original_preword').show();
                          $('.mod_solo_corrections_cont .mod_solo_grading_original_postword').show();
                      }
                  });
            });
{{/js}}